#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Автоматически исправляем markdown файлы
echo "🔧 Исправляем markdown ошибки..."
npx lint-staged

# Проверяем, что все исправлено
echo "✅ Проверяем результат..."
npx markdownlint '**/*.md'
